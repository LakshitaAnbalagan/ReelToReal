import { Link } from 'react-router-dom'; const art = ['#f4c95d', '#a3d8c4', '#c9b7e7', '#fbac9b', '#9dc3e6']; export default function VideoCard({ video, index = 0 }) { return <Link className="video-card" to={`/library/${video._id}`}><div className="card-art" style={{ background: art[index % art.length] }}><span>{video.category === 'Food' || video.category === 'Recipes' ? '✦' : video.category === 'Travel' ? '◒' : '↗'}</span><small>{video.isDemo ? 'DEMO MEMORY' : 'SAVED MEMORY'}</small></div><div className="card-body"><div className="eyebrow">{video.category}</div><h3>{video.title}</h3><p>{video.summary}</p><div className="tags">{video.tags?.slice(0, 3).map((tag) => <span key={tag}>#{tag}</span>)}</div></div></Link>; }
