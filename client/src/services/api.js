import axios from 'axios'; const api = axios.create({ baseURL: '/api' }); export const videosApi = { list: (category) => api.get('/videos', { params: category ? { category } : {} }), get: (id) => api.get(`/videos/${id}`), upload: (data) => api.post('/videos/upload', data), fromUrl: (url) => api.post('/videos/url', { url }), process: (id) => api.post(`/videos/${id}/process`), remove: (id) => api.delete(`/videos/${id}`) }; export const plannerApi = { query: (query) => api.post('/planner/query', { query }), chat: (data) => api.post('/planner/chat', data) }; export const authApi = { register: (data) => api.post('/auth/register', data), login: (data) => api.post('/auth/login', data), getMe: (token) => api.get('/auth/me', { headers: { Authorization: `Bearer ${token}` } }) }; export const categoriesApi = () => api.get('/categories'); export default api;
