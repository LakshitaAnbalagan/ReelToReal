import axios from 'axios'; const api = axios.create({ baseURL: '/api' }); export const videosApi = { list: (category) => api.get('/videos', { params: category ? { category } : {} }), get: (id) => api.get(`/videos/${id}`), upload: (data) => api.post('/videos/upload', data), fromUrl: (url) => api.post('/videos/url', { url }), process: (id) => api.post(`/videos/${id}/process`), remove: (id) => api.delete(`/videos/${id}`) }; export const plannerApi = { query: (query) => api.post('/planner/query', { query }) }; export const categoriesApi = () => api.get('/categories'); export default api;
